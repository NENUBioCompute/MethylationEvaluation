# ============================================
# DNAmClocksEvaluation Requirements
# ============================================
# This project requires both Python and R environments.
# Please ensure that both are installed before running the tool.
# Python version: >= 3.9
# R version:     >= 4.5
# --------------------------------------------

# === Python Dependencies ===
tensorflow==2.15.0
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.6.0
EpigeneticPacemaker

# === Optional Utilities (recommended) ===
# rpy2          # optional: for bridging Python <-> R if not already handled

# ============================================
# === R Dependencies (to be installed manually) ===
# ============================================
# Open R console and run the following commands:

# install.packages("BiocManager")
# BiocManager::install("MEAT")
# BiocManager::install("methylclock")

# --------------------------------------------
# Alternatively, you can use Rscript:
# Rscript -e "if (!requireNamespace('BiocManager', quietly=TRUE)) install.packages('BiocManager'); \
#             BiocManager::install(c('MEAT', 'methylclock'))"
# --------------------------------------------
